{{#if errors}}
<div class="my-3 bg-rose-500 py-2 px-5 text-rose-900 opacity-90 font-semibold text-lg rounded-2xl max-w-2xl">
    <ul class="mx-5 list-disc">
        {{#each errors as |error|}}
        <li>{{ error.msg }}</li>
        {{/each}}
    </ul>
</div>
{{/if}}