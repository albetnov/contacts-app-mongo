{{#if msg}}
{{#if (alert variant)}}
<div class="my-3 bg-green-500 py-2 px-5 text-green-900 opacity-90 font-semibold text-lg rounded-2xl max-w-2xl">{{ msg }}
</div>
{{else}}
<div class="my-3 bg-rose-500 py-2 px-5 text-rose-900 opacity-90 font-semibold text-lg rounded-2xl max-w-2xl">{{ msg }}
</div>
{{/if}}
{{/if}}